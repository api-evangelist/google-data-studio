naftiko: 1.0.0-alpha2
info:
  label: Google Data Studio API — Permissions
  description: 'Google Data Studio API — Permissions. 4 operations. Lead operation: Google Data Studio Get asset permissions.
    Self-contained Naftiko capability covering one Google Data Studio business surface.'
  tags:
  - Google Data Studio
  - Permissions
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GOOGLE_DATA_STUDIO_API_KEY: GOOGLE_DATA_STUDIO_API_KEY
capability:
  consumes:
  - type: http
    namespace: google-data-studio-permissions
    baseUri: https://datastudio.googleapis.com/v1
    description: Google Data Studio API — Permissions business capability. Self-contained, no shared references.
    resources:
    - name: assets-assetName-permissions
      path: /assets/{assetName}/permissions
      operations:
      - name: getpermissions
        method: GET
        description: Google Data Studio Get asset permissions
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: patchpermissions
        method: PATCH
        description: Google Data Studio Update asset permissions
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: assets-assetName-permissions:addMembers
      path: /assets/{assetName}/permissions:addMembers
      operations:
      - name: addmembers
        method: POST
        description: Google Data Studio Add members to an asset
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: assets-assetName-permissions:revokeAllPermissions
      path: /assets/{assetName}/permissions:revokeAllPermissions
      operations:
      - name: revokeallpermissions
        method: POST
        description: Google Data Studio Revoke all permissions for members
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.GOOGLE_DATA_STUDIO_API_KEY}}'
  exposes:
  - type: rest
    namespace: google-data-studio-permissions-rest
    port: 8080
    description: REST adapter for Google Data Studio API — Permissions. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/assets/{assetname}/permissions
      name: assets-assetname-permissions
      description: REST surface for assets-assetName-permissions.
      operations:
      - method: GET
        name: getpermissions
        description: Google Data Studio Get asset permissions
        call: google-data-studio-permissions.getpermissions
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patchpermissions
        description: Google Data Studio Update asset permissions
        call: google-data-studio-permissions.patchpermissions
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/assets/{assetname}/permissions-addmembers
      name: assets-assetname-permissions-addmembers
      description: REST surface for assets-assetName-permissions:addMembers.
      operations:
      - method: POST
        name: addmembers
        description: Google Data Studio Add members to an asset
        call: google-data-studio-permissions.addmembers
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/assets/{assetname}/permissions-revokeallpermissions
      name: assets-assetname-permissions-revokeallpermissions
      description: REST surface for assets-assetName-permissions:revokeAllPermissions.
      operations:
      - method: POST
        name: revokeallpermissions
        description: Google Data Studio Revoke all permissions for members
        call: google-data-studio-permissions.revokeallpermissions
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: google-data-studio-permissions-mcp
    port: 9090
    transport: http
    description: MCP adapter for Google Data Studio API — Permissions. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: google-data-studio-get-asset
      description: Google Data Studio Get asset permissions
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: google-data-studio-permissions.getpermissions
      outputParameters:
      - type: object
        mapping: $.
    - name: google-data-studio-update-asset
      description: Google Data Studio Update asset permissions
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: google-data-studio-permissions.patchpermissions
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: google-data-studio-add-members
      description: Google Data Studio Add members to an asset
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: google-data-studio-permissions.addmembers
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: google-data-studio-revoke-all
      description: Google Data Studio Revoke all permissions for members
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: google-data-studio-permissions.revokeallpermissions
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
